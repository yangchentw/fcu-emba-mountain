if (matchMedia('(prefers-reduced-motion: no-preference)').matches) new IntersectionObserver((entries) => entries.forEach((entry) => entry.isIntersecting && entry.target.classList.add('is-visible')), { threshold: .15 }).observe && document.querySelectorAll('.reveal').forEach((item) => new IntersectionObserver((entries) => entries.forEach((entry) => entry.isIntersecting && entry.target.classList.add('is-visible')), { threshold: .15 }).observe(item));
